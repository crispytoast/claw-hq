node_modules/
dist/
*.tgz
.env
.env.local
*.log
.DS_Store

# Tunnel agent operator token — never commit. Use config.example.json as the template.
apps/tunnel-agent/config.json

# Runtime / state — never commit
.runtime/
*.db
*.sqlite
*.sqlite3
apps/cloud-relay/data/
apps/cloud-relay/push-config.json
apps/cloud-relay/*.db

# Android build artifacts
apps/android/.gradle/
apps/android/build/
apps/android/app/build/
apps/android/*.keystore
apps/android/local.properties
apps/android/app/google-services.json

# Firebase service account — never commit
**/.firebase-sa.json
**/firebase-service-account*.json
